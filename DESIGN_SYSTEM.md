# SEBU Design System

SEBU 프론트엔드가 따르는 디자인 규격 문서예요.
화면으로 확인하려면 `npm run dev` 후 **`/design-system`** 으로 들어가세요.

**값의 출처**: Figma Make 내보내기 — `세부 와이어프레임 (Community)` 의
`src/index.css` + `src/App.tsx`

| 무엇 | 어디 |
|---|---|
| 실제 토큰 값 (HEX, radius, motion …) | `src/index.css` 의 `@theme` 블록 — **여기가 유일한 정의처** |
| 쇼케이스에 표시할 목록·설명 | `src/constants/designTokens.js` |
| 눈으로 보는 레퍼런스 페이지 | `src/pages/DesignSystem/` → `/design-system` |
| 왜 이렇게 정했는지 (이 문서) | `DESIGN_SYSTEM.md` |

> 토큰을 추가·수정할 때는 **`index.css` → `designTokens.js` → 해당 section 파일 → 이 문서**
> 순서로 손대세요. 값을 여러 곳에 복사해두면 금방 어긋납니다.

---

## §0. 먼저 읽어야 할 것 — 브랜드 컬러 이슈

Figma 원본의 `src/index.css`는 이렇게 되어 있었습니다.

```css
--color-toss-blue: #3182F6;
--color-toss-blue-light: #EBF3FF;
--color-gray-900: #191F28;  /* 이하 회색 스케일 전체 */
```

**`#3182F6`은 토스의 브랜드 컬러**이고, 회색 스케일(`#191F28`, `#4E5968`, `#6B7684`,
`#8B95A1`, `#B0B8C1`, `#D1D6DB`, `#E5E8EB`, `#F2F4F6`, `#F9FAFB`)도 토스가 쓰는 값과
같습니다. 토큰 이름까지 `toss-blue`였고요.

### 지금 어떻게 해뒀나

- 토큰 이름을 **역할 기반**으로 바꿨습니다: `toss-blue` → `brand-500`
- **HEX 값은 Figma 그대로** 두었습니다 (디자인을 그대로 반영하라는 요청에 따라)

### 왜 짚고 넘어가야 하나

색상 HEX 값 자체는 일반적으로 저작권 대상이 아니지만, 두 가지가 다릅니다.

1. **브랜드 컬러**는 성격이 달라요. 다른 회사의 대표색을 우리 서비스의 대표색으로
   쓰는 건 저작권보다 **상표·브랜드 혼동** 쪽 문제에 가깝습니다. SEBU는 앱인토스가
   아닌 독립 서비스라 "토스 관련 서비스처럼 보이는" 상황을 만들 수 있어요.
2. **회색 스케일은 위험도가 훨씬 낮습니다.** 중립 회색은 브랜드 식별력이 없고
   기능적인 값이라, 그대로 써도 실질적인 문제가 되기 어렵습니다.

### 권하는 방향

`brand-500`만 SEBU 고유의 색으로 옮기는 걸 권합니다. 파랑 계열을 유지하되 색상값을
옮기면 화면 인상은 거의 그대로면서 남의 브랜드 컬러를 쓰는 상태는 벗어날 수 있어요.
바꿀 때는 `index.css`의 brand 6줄만 고치면 서비스 전체에 반영됩니다.

> 이 문서는 공개된 약관을 읽고 정리한 것이지 법률 자문이 아니에요.
> 실제 배포 전에는 팀에서 한 번 결정하고 넘어가 주세요.

### 그 외에 토스에서 가져온 것 / 안 가져온 것

공개 문서(개발자센터 `.md`, 기술블로그)에 게시된 **일반 원칙**만 참고했습니다.

| 가져온 것 | 어떻게 반영했나 |
|---|---|
| 시맨틱 토큰 네이밍 (역할 기반) | `brand-*`, `rank-first-bg`, `status-open` |
| UX 라이팅 5원칙 | §6 |
| 다크패턴 방지 | §7 |
| "화면당 핵심 강조는 하나만" | 순위 1·2·3위만 색, 4위 이하 중립 |

**안 가져온 것**: TDS Figma UI Kit 파일, 토스 아이콘·이모지 세트(문서에
*"앱인토스 제휴 환경 내에서만 사용"* 명시), 미니앱 전용 규격(360–420px 논리 해상도,
내비게이션 바/탭바 컴포넌트).

---

## §1. 적용 원칙 — Figma와 코드가 다를 때

Figma가 코드보다 앞서 있어서 둘이 다릅니다. 기준은 이거예요.

> **범위(무엇을 만들지)는 코드 기준.**
> Figma에 있어도 아직 구현 안 된 기능은 가져오지 않습니다.
>
> **스타일(어떻게 보일지)은 Figma 기준.**
> 이미 구현된 기능은 Figma의 디자인과 인터랙션을 그대로 따릅니다.

| 영역 | Figma | 현재 코드 | 판단 |
|---|---|---|---|
| 상단 내비게이션 | 3개 항목 + hover 메가메뉴 + 딤 오버레이 + 우측 아이콘 3개 | ✅ 적용 완료 — 항목은 '단과대별 보기' 1개 | **스타일 반영** — 높이·타이포·hover 전환·CTA는 Figma대로. 메가메뉴와 우측 아이콘은 해당 기능이 생길 때 (아래 참고) |
| 검색 필터 탭 | 단과대/학과 · 연구 분야 · 지원 자격 · 모집 상태 | 단과대/학과만 (모집 상태는 주석) | **일부만** — 탭 하나의 생김새만 반영, 나머지 3개는 기능이 생길 때 |
| 단과대 칩 | pill, 선택 시 파란 배경+체크+테두리 | 동일 구조로 구현됨 | **스타일 반영** — 미선택 칩 테두리 버그만 수정 필요 (아래 §8) |
| 실시간 인기 연구실 | 축소 티커(40px) ↔ 펼침 카드(16px) 토글, 2.8초 롤링 | ✅ 적용 완료 — `RecommendedLabs.jsx` | **스타일 반영** — 데이터·동작(북마크 수 상위 5개, 클릭 시 상세 모달, hover 시 정지)은 기존 로직 그대로 |
| 로그인 페이지 | 최소 헤더(뒤로가기+로고), 카드형 폼, 라벨+플레이스홀더, 안내 박스 | ✅ 적용 완료 — `Login/index.jsx` + `LoginForm.jsx` | **스타일 반영** — 검증·포커스 이동·에러·로딩 로직은 그대로. 비밀번호 표시 토글만 새로 추가 |
| 모집 상태 배지 | 상시모집(파랑+점) / 마감(회색) | import가 주석 처리됨 | **일부만** — 토큰만 정의, 주석 풀 때 사용 |
| 커뮤니티·튜토리얼 하위 페이지 | 연구실 후기·인기글·Q&A·자기소개서·컨택 메일 | 라우트 없음 | **제외** — 페이지가 없으니 메가메뉴 항목도 안 만듦 |
| 마이페이지·메일함·가이드 | 전용 페이지 존재 | `MyPageSetup`만 최소 구현 | **제외** — 해당 화면 만들 때 다시 참고 |

---

## §2. Color

### Brand

| 토큰 | HEX | 용도 |
|---|---|---|
| `brand-50` | `#EBF3FF` | 활성 탭·선택 칩·hover 배경 |
| `brand-100` | `#DBEAFE` | 옅은 강조 배경 |
| `brand-200` | `#BFDBFE` | 배지 테두리·포커스 링 |
| `brand-500` | `#3182F6` | **Primary** — CTA·로고·활성 탭 ⚠️ §0 |
| `brand-600` | `#1D4ED8` | Hover |
| `brand-700` | `#1E40AF` | Active / Pressed |

### Neutral

Tailwind 기본 회색 대신 **Figma 회색 스케일로 덮어썼습니다.**
기존 컴포넌트가 쓰던 `gray-*` 클래스가 그대로 새 팔레트를 따라가요.

`50` #F9FAFB · `100` #F2F4F6 · `200` #E5E8EB · `300` #D1D6DB · `400` #B0B8C1 ·
`500` #8B95A1 · `600` #6B7684 · `700` #4E5968 · `800` #333D4B · `900` #191F28

### Feature — 실시간 인기 연구실

| 토큰 | HEX | 용도 |
|---|---|---|
| `live` / `live-bg` | `#3182F6` / `#EBF3FF` | '실시간' 라벨 (축소 상태) |
| `live-dot` | `#EF4444` | 제목 옆 빨간 점 (펼친 상태) |
| `rank-first` / `-bg` / `-line` | `#E53935` / `#FFF1F0` / `#FFCDD2` | 1위 |
| `rank-second` / `-bg` / `-line` | `#E07B00` / `#FFF8E6` / `#FFE0B2` | 2위 |
| `rank-third` / `-bg` / `-line` | `#6B7280` / `#F3F4F6` / `#E5E7EB` | 3위 |
| `rank-rest` / `-bg` | `#9CA3AF` / `#F9FAFB` | 4위 이하 |
| `ticker-first` / `-second` / `-rest` | `#EF4444` / `#F97316` / `#9CA3AF` | 축소 티커의 순위 숫자 |

배지(펼친 상태)와 티커(축소 상태)의 순위 색이 서로 다릅니다 — Figma가 그렇게 되어 있어요.

### Status — 모집 상태

`status-open` #3B82F6 / `-bg` #EFF6FF / `-line` #BFDBFE / `-dot` #60A5FA
`status-closed` #9CA3AF / `-bg` #F9FAFB / `-line` #E5E7EB

---

## §3. Radius

| 토큰 | px | 용도 |
|---|---|---|
| `rounded-control` | 12 | 내비 항목·아이콘 버튼·메가메뉴 항목 |
| `rounded-card` | 16 | 카드·위젯(펼침) |
| `rounded-ticker` | 40 | 위젯(축소) |
| `rounded-full` | 999 | CTA·칩·배지 |

## §4. Typography

폰트는 **Noto Sans KR** (Google Fonts). Figma와 동일하게 맞췄어요.

Figma는 `13.5px`, `10.5px` 같은 소수점 크기를 씁니다. Tailwind 기본 스케일에 없는
값이라 임의값 표기(`text-[13.5px]`)로 씁니다.

`10` 메가메뉴 라벨 · `10.5` 티커 라벨/학과명 · `11` 캡션 · `12` 티커 연구실명 ·
`13` 위젯 연구실명/CTA · `13.5` 내비 항목 · `14` 위젯 제목 · `20` 로고

## §5. Motion

| 대상 | 값 |
|---|---|
| 내비 항목 전환 | 150ms |
| chevron 회전 | 150ms (180deg) |
| 위젯 radius 전환 | 220ms ease (40px ↔ 16px) |
| 티커 페이드 | 220ms ease — opacity + translateY(5px) |
| 티커 교체 딜레이 | 240ms |
| 티커 주기 | **2800ms** |
| 라이브 점 확산 | `ping-slow` 1.4s `cubic-bezier(0,0,.2,1)` infinite |

자동으로 움직이는 콘텐츠에는 두 가지가 **필수**예요.

1. **마우스를 올리면 멈춘다** — 읽는 도중 넘어가면 안 됩니다.
2. **`prefers-reduced-motion`에서 정지** — `index.css`에 전역으로 처리해뒀어요.
   자동 움직임은 전정기관이 예민한 사용자에게 실제로 어지럼증을 유발합니다.

---

## §6. UX 라이팅

| 원칙 | 권장 | 지양 |
|---|---|---|
| 해요체로 통일 | 연구실 1개를 찾았어요 | 연구실 1개가 검색되었습니다 |
| 능동형 (`~되었다` 피하기) | 북마크에 담았어요 | 북마크에 담아졌어요 |
| 긍정형 | 로그인하면 볼 수 있어요 | 로그인하지 않으면 볼 수 없어요 |
| 다이얼로그 왼쪽 버튼 | **닫기** / 확인 | 취소 / 확인 |
| 과한 경어 빼기 | 연구실을 찾아볼까요? | 연구실을 찾아보시겠어요? |
| 명사 나열 대신 풀어쓰기 | 지원 자격을 확인해보세요 | 지원자격조건정보 확인 |

'취소'를 쓰지 않는 이유: 사용자가 *진행 중이던 작업이 취소된다*고 오해합니다.

## §7. 하지 말 것 (다크패턴)

- 서비스 진입 직후 전면 바텀시트 띄우기
- 뒤로 가기를 눌렀을 때 가로막는 바텀시트
- 거절 선택지가 없는 CTA (닫을 방법을 항상 제공)
- 흐름 중 갑자기 등장하는 광고
- 눌렀을 때 무슨 일이 일어나는지 알 수 없는 버튼 문구

---

## §8. 다음에 할 일

- [ ] **`brand-500`을 SEBU 고유 색으로 옮길지 결정** (§0)
- [x] ~~`Header.jsx`를 Figma 스타일로 교체~~ — 완료

### 헤더에서 의도적으로 뺀 것

Figma에는 있지만 대응하는 기능이 없어 넣지 않았습니다. 기능이 생기면 그때 추가해요.

| 뺀 것 | 다시 넣는 시점 |
|---|---|
| '커뮤니티' 항목 + 메가메뉴 (커뮤니티 홈·연구실 후기·인기글·Q&A) | 커뮤니티 라우트가 생길 때 |
| '튜토리얼' 항목 + 메가메뉴 (자기소개서·컨택 메일·서비스 사용 방법) | 해당 페이지가 생길 때 |
| 우측 아이콘 3개 (검색·알림·북마크) | 각 기능이 붙을 때. Figma 규격은 `w-9 h-9 rounded-control`, 알림 점은 `#EF4444` |
| 메가메뉴 열 정렬 그리드 | 메가메뉴를 넣을 때. Figma는 헤더와 패널이 `1fr auto 1fr` + `repeat(3, 160px)`를 **똑같이** 써서 열을 맞춥니다 |
| 딤 오버레이 `rgba(0,0,0,0.15)` · 패널 그림자 `0 16px 48px rgba(0,0,0,0.08)` | 위와 동일 |
- [ ] `CollegeChips.jsx` 버그 — 미선택 칩에도 `border-blue-200`이 들어가 있어요.
      미선택은 투명 테두리가 맞습니다
- [x] ~~`RecommendedLabs`를 축소 티커 ↔ 펼침 카드 위젯으로 교체~~ — 완료
- [x] ~~로그인 페이지~~ — 완료
- [ ] 남은 `blue-*` 하드코딩을 `brand-*`로 이전
      (`SearchBar`, `FilterTabs`, `CollegeChips`, `LabCard`, `LabDetailModal`, `ComingSoonModal`)
- [ ] `PopularPosts`는 아직 회색 박스 placeholder — 커뮤니티 기능이 생길 때
- [ ] `RECRUITMENT_STATUS` 주석 해제 시 `status-*` 토큰 적용
- [ ] `src/App.css` 는 어디서도 import되지 않는 Vite 템플릿 잔재예요. 삭제해도 됩니다
